# PTY Recording Fixtures

Place minimized `.pty.bin` recordings in this directory, with the matching `.pty.json`
sidecar when available. If an expected terminal snapshot has been reviewed against a
known-good terminal, store it beside the recording as `.pty.expected.json`.
